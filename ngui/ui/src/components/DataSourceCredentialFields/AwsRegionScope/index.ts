import AwsRegionScope from "./AwsRegionScope";
import { FIELD_NAMES, REGION_SCOPE_MODES } from "./constants";
import { getRegionScopeDefaultValues, getRegionScopeConfigParams } from "./utils";

export {
  FIELD_NAMES as AWS_REGION_SCOPE_FIELD_NAMES,
  REGION_SCOPE_MODES,
  getRegionScopeDefaultValues,
  getRegionScopeConfigParams,
};
export default AwsRegionScope;
